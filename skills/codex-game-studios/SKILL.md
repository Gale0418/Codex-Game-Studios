---
name: codex-game-studios
description: Use for Codex-first game project work that needs routing, parallel agents, QA, release checks, or the packaged Codex Game Studios plugin entry.
---

# Codex Game Studios Plugin Entry

Read `../../SKILL.md` completely before taking task actions. The root skill file is the canonical workflow and keeps this plugin entry small.

Resolve bundled resources from the plugin root after reading the canonical skill:

- `../../commands/` for command entry points
- `../../workflows/` for routed workflow steps
- `../../references/` for routing, multi-agent, and governance docs
- `../../templates/` for handoff and wave-plan outputs
- `../../scripts/` for validation and derived-doc generation

If the canonical root skill is unavailable, stop and report that the plugin package is incomplete instead of inventing studio steps.
