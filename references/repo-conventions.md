# Repo Conventions

- Respect the repo’s current file layout and naming style.
- Prefer minimal diffs over broad refactors.
- Keep game rules centralized instead of scattering constants across UI code.
- If the repo already uses a worker or helper file for AI or heavy logic, keep that division intact.
- Do not rewrite assets or generated content unless the task explicitly asks for it.

